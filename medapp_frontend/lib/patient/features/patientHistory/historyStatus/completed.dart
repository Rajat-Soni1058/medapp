import 'package:flutter/material.dart';

class CompletedStatus extends StatelessWidget {
  const CompletedStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Completed'),
    );
  }
}